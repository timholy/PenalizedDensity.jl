var documenterSearchIndex = {"docs":
[{"category":"section","location":"reference/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"type","location":"reference/#PenalizedDensity.PenalizedDensityEstimate","page":"API reference","text":"PenalizedDensityEstimate(x::AbstractVector{T}; κ, rtol=cbrt(eps(T)))\n\nEstimate a continuous one-dimensional probability density from sample points x, using the scalar-field method of Holy, Phys. Rev. Lett. 79, 3545 (1997).\n\nThe density is written as Q(x) = ψ(x)^2, where the amplitude ψ minimizes the action\n\nS[ψ] = ∫ (ℓ²/2) (ψ')² dx - 2 Σᵢ ln ψ(xᵢ)\n\nsubject to ∫ ψ² dx = 1. The smoothing scale κ = √(2λ)/ℓ (with λ the normalization multiplier) sets the width of each point's contribution; larger κ gives a rougher estimate. See select_kappa for choosing it automatically.\n\nBetween sorted data points ψ solves ψ'' = κ² ψ, i.e. it is a sum of rising and falling exponentials, and decays as e^{-κ|x|} in the tails. The nodal amplitudes ψ(xᵢ) satisfy a symmetric tridiagonal system whose solution is the minimizer of a strictly convex potential; normalization is then a rescaling.\n\nThe returned object is callable: d(x) evaluates the density Q(x) at any real x, and it can be broadcast over arrays. Use amplitude for ψ(x).\n\nRepeated points, and points closer than rtol / κ (i.e. within a fraction rtol of the smoothing length 1 / κ), are merged into one node carrying the count as its integer weight, so weighted data is handled naturally. Without merging, the resulting tridiagonal system can be nearly singular.\n\nExamples\n\njulia> d = PenalizedDensityEstimate([-1.0, 0.0, 0.0, 1.0]; κ=1.0);\n\njulia> d(0.0) > d(2.0)   # denser near the data\ntrue\n\njulia> round(chisq(d, d); digits=8)   # a distribution fits itself perfectly\n0.0\n\n\n\n\n\n","title":"PenalizedDensity.PenalizedDensityEstimate"},{"category":"method","location":"reference/#PenalizedDensity._merge_sorted-Union{Tuple{T}, Tuple{AbstractVector, T, Type{T}}} where T","page":"API reference","text":"_merge_sorted(x, atol, T) -> (nodes, weights)\n\nSort x and collapse runs of points within atol of the run's first member into a single node carrying the count as its weight. Returns distinct, strictly increasing nodes::Vector{T} and matching weights, regardless of the axes of x.\n\n\n\n\n\n","title":"PenalizedDensity._merge_sorted"},{"category":"method","location":"reference/#PenalizedDensity._solve_amplitude-Union{Tuple{T}, Tuple{LinearAlgebra.SymTridiagonal{T, V} where V<:AbstractVector{T}, Vector{T}}} where T<:AbstractFloat","page":"API reference","text":"_solve_amplitude(M, w)    -> ψ\n_solve_amplitude(x, w, κ) -> ψ\n\nMinimize the strictly convex potential F(ψ) = ½ ψ'Mψ - Σ wᵢ ln ψᵢ over ψ > 0 by a damped Newton iteration with an SPD tridiagonal Hessian. The minimizer solves Mψ = w ./ ψ, i.e. the field equation at unit multiplier; the caller rescales it to impose normalization.\n\nEach step factorizes the tridiagonal Hessian in place (ldlt!/ldiv!) and backtracks along the Newton direction to keep ψ > 0 with Armijo decrease. Iteration stops when the Newton decrement λ² = ∇FᵀΔ drops below a relative tolerance, or when the line search can no longer decrease F.\n\n\n\n\n\n","title":"PenalizedDensity._solve_amplitude"},{"category":"method","location":"reference/#PenalizedDensity.action-Tuple{PenalizedDensityEstimate}","page":"API reference","text":"action(d::PenalizedDensityEstimate) -> S\n\nClassical action S[ψ_cl] = N - λ - Σᵢ wᵢ ln Q(xᵢ) (Eq. 10) of the fitted density, where N = Σ wᵢ. Used by select_kappa.\n\n\n\n\n\n","title":"PenalizedDensity.action"},{"category":"method","location":"reference/#PenalizedDensity.amplitude-Tuple{PenalizedDensityEstimate, Real}","page":"API reference","text":"amplitude(d::PenalizedDensityEstimate, x)\n\nEvaluate the amplitude ψ(x) (so that the density is d(x) == ψ(x)^2) at real x, which may be a scalar or an array.\n\n\n\n\n\n","title":"PenalizedDensity.amplitude"},{"category":"method","location":"reference/#PenalizedDensity.chisq-Union{Tuple{T}, Tuple{PenalizedDensityEstimate{T}, Any}} where T","page":"API reference","text":"chisq(d::PenalizedDensityEstimate, Q) -> χ²\n\nGoodness-of-fit statistic between a trial density Q and the data underlying the fit d, the robust field-theoretic analogue of Pearson's χ² (Eqs. 13–14 of the paper):\n\nχ² = 4 Σᵢ wᵢ (√Q(xᵢ) / ψ_cl(xᵢ) - 1)²,\n\nsummed over the data nodes xᵢ with multiplicities wᵢ, where ψ_cl = √(d(·)) is the fitted amplitude. Q is any callable returning density values; it should be a normalized density (∫Q dx = 1). chisq(d, d) == 0. Small χ² means Q is close to the data in the (squared Hellinger) sense; see pvalue and chisq_ccdf for significance.\n\n\n\n\n\n","title":"PenalizedDensity.chisq"},{"category":"method","location":"reference/#PenalizedDensity.chisq_ccdf-Union{Tuple{T}, Tuple{PenalizedDensityEstimate{T}, Real}} where T","page":"API reference","text":"chisq_ccdf(d::PenalizedDensityEstimate, z) -> P(χ² ≥ z)\n\nUpper-tail (complementary CDF, i.e. survival) probability of the reference χ² distribution chisq_pdf. Evaluating it at an observed statistic gives a p-value; see pvalue.\n\n\n\n\n\n","title":"PenalizedDensity.chisq_ccdf"},{"category":"method","location":"reference/#PenalizedDensity.chisq_pdf-Union{Tuple{T}, Tuple{PenalizedDensityEstimate{T}, Real}} where T","page":"API reference","text":"chisq_pdf(d::PenalizedDensityEstimate, z) -> P(z)\n\nDensity of the reference χ² distribution at z ≥ 0 in the large-N limit (Eq. 26): the inverse-Gaussian (Wald) law with mean ⟨χ²⟩ = expected_chisq(d) and shape ⟨χ²⟩²,\n\nP(z) = ⟨χ²⟩ / √(2π z³) · exp[⟨χ²⟩ - z/2 - ⟨χ²⟩²/(2z)].\n\n\n\n\n\n","title":"PenalizedDensity.chisq_pdf"},{"category":"method","location":"reference/#PenalizedDensity.expected_chisq-Union{Tuple{PenalizedDensityEstimate{T}}, Tuple{T}} where T","page":"API reference","text":"expected_chisq(d::PenalizedDensityEstimate) -> ⟨χ²⟩\n\nMean of the reference χ² distribution in the large-N limit (Eq. 25), ⟨χ²⟩ = κ X / √2, where X = (1/N) Σᵢ wᵢ / Q_cl(xᵢ) estimates the size of the region occupied by the data. With 1/κ read as an effective bin width this is about 1/√2 ≈ 0.7 per degree of freedom.\n\n\n\n\n\n","title":"PenalizedDensity.expected_chisq"},{"category":"method","location":"reference/#PenalizedDensity.kappa_interval-Tuple{AbstractVector{<:Real}}","page":"API reference","text":"kappa_interval(x; level=0.2, rtol=1e-6) -> (; κ, lo, hi)\n\nPrincipled smoothing-scale selection with an interval of plausible values.\n\nAs κ sweeps from 0 to ∞ the classical action's reduced form g(κ) = S(κ) + W ln κ (with W = Σ wᵢ the total count) rises monotonically between two exact limits: g → W/2 as κ → 0 (all points merge into one lump) and g → W/2 + W H as κ → ∞ (the N points become isolated), where H = -Σᵢ (wᵢ/W) ln(wᵢ/W) is the Shannon entropy of the multiplicities (ln N for distinct points). The normalized quantity\n\nh(κ) = (g(κ) - W/2) / (W H) ∈ [0, 1]\n\nis therefore the fraction of the data's entropy that scale κ resolves, and its half-point h = 1/2 is returned as κ. This entropy criterion is distinct from the minimum-sensitivity scale of select_kappa; one advantage of this function is that it doesn't require computing a noisy numerical derivative.\n\nReturns the half-entropy scale κ (h = 1/2) together with the interval [lo, hi] bracketing h ∈ [(1-level)/2, (1+level)/2]; the default level=0.2 spans h ∈ [0.4, 0.6]. Requires at least two distinct points.\n\n\n\n\n\n","title":"PenalizedDensity.kappa_interval"},{"category":"method","location":"reference/#PenalizedDensity.pvalue-Tuple{PenalizedDensityEstimate, Any}","page":"API reference","text":"pvalue(d::PenalizedDensityEstimate, Q) -> p\n\nSignificance of the fit of a trial density Q: the probability that the reference χ² distribution exceeds the observed chisq(d, Q), i.e. chisq_ccdf(d, chisq(d, Q)). Valid in the large-N limit.\n\n\n\n\n\n","title":"PenalizedDensity.pvalue"},{"category":"method","location":"reference/#PenalizedDensity.select_kappa-Tuple{AbstractVector{<:Real}}","page":"API reference","text":"select_kappa(x; κs=<data-scaled grid>, rtol=1e-6) -> κ\n\nChoose the smoothing scale by the principle of minimum sensitivity: return the κ at which the classical action action S is least sensitive to the scale, i.e. |dS/d ln κ| is smallest (Fig. 1 of the paper). The derivative dS/d ln κ is evaluated analytically and minimized over κ by a golden-section search, bracketed by the grid κs (which defaults to a geometric range scaled to the data's extent).\n\nThis is a principled convention rather than a unique optimum: S has no exact stationary point in κ, so the flattest point depends on measuring sensitivity in ln κ. It generally selects a different scale than the entropy-based kappa_interval, and neither targets minimum integrated squared error, but see select_kappa_cv.\n\nκs must be sorted and positive, with at least three values to bracket the minimum.\n\n\n\n\n\n","title":"PenalizedDensity.select_kappa"},{"category":"method","location":"reference/#PenalizedDensity.select_kappa_cv-Tuple{AbstractVector{<:Real}}","page":"API reference","text":"select_kappa_cv(x; κs=<data-scaled grid>, rtol=1e-6) -> κ\n\nChoose the smoothing scale by least-squares cross-validation: return the κ minimizing\n\nLSCV(κ) = ∫ Q̂_κ(x)² dx - (2/N) Σᵢ Q̂_{κ,-i}(xᵢ),\n\nan unbiased estimate — up to the κ-independent ∫Q² — of the integrated squared error ∫(Q̂_κ - Q)², where Q̂_{κ,-i} is the density fitted with the i-th point left out. Its minimizer therefore targets minimum mean integrated squared error (MISE). This generally selects a finer scale than select_kappa (minimum sensitivity) and kappa_interval (half-entropy), which resolve information rather than squared error and tend to over-resolve smooth densities.\n\nBoth terms are evaluated analytically in O(N): ∫Q̂² in closed form over the exponential segments, and each leave-one-out density Q̂_{-i}(xᵢ) from a first-order expansion of the fit in the dropped point's weight, so no per-point refitting is needed. The score is minimized by a golden-section search over ln κ, bracketed by the grid κs (a geometric range scaled to the data's extent by default).\n\nCross-validation assumes the data are draws from a continuous density. Heavily tied or coarsely rounded data instead resemble a discrete distribution, for which LSCV decreases without bound as κ → ∞ (finer scales keep resolving the atoms); select_kappa_cv then returns a large κ. Prefer select_kappa or kappa_interval, which stay bounded, in that regime.\n\nκs must be sorted and positive, with at least three values to bracket the minimum.\n\n\n\n\n\n","title":"PenalizedDensity.select_kappa_cv"},{"category":"section","location":"#PenalizedDensity","page":"Home","text":"PenalizedDensity estimates a continuous one-dimensional probability density from sample points, without binning, using the penalized maximum-likelihood scalar-field method of\n\nT. E. Holy, \"Analysis of Data from Continuous Probability Distributions,\" Phys. Rev. Lett. 79, 3545 (1997).\n\nThe density is written Q(x) = psi(x)^2, and the amplitude psi maximizes the likelihood of the data subject to a smoothness penalty and normalization int Qdx = 1. Each data point contributes a peak of width sim 1kappa; the single smoothing scale kappa trades resolution against smoothness. Sorting the data and writing psi as rising and falling exponentials between adjacent points reduces the estimator to a symmetric tridiagonal problem, solved by a convex Newton iteration in O(N) time with normalization imposed by a single rescaling.","title":"PenalizedDensity"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"PenalizedDensity\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using PenalizedDensity\n\nx = [-2.1, -0.4, -0.4, 0.3, 1.2, 1.9]     # samples from an unknown distribution\n\nd = PenalizedDensityEstimate(x; κ = 1.0)  # callable: d(x) is the density Q(x)\n(d(0.0), d(2.0))\n\nThe result is callable and returns the estimated density; amplitude gives psi(x) = sqrtQ(x).\n\nThe Tutorial works through choosing the smoothing scale and testing a model against the data; the API reference documents every function.","title":"Quick start"},{"category":"section","location":"tutorial/#Tutorial","page":"Tutorial","text":"This walkthrough fits a bimodal distribution, chooses the smoothing scale, and tests candidate models against the data.","title":"Tutorial"},{"category":"section","location":"tutorial/#A-worked-example","page":"Tutorial","text":"Consider a mixture of two Gaussians with equal weight, a narrow one (sigma=04) centered at x=-2 and a broad one (sigma=12) at x=3:\n\nusing PenalizedDensity, Random\n\nRandom.seed!(42)\ncomps = [(w=0.5, μ=-2.0, σ=0.4), (w=0.5, μ=3.0, σ=1.2)]\ntruepdf(x) = sum(c.w * exp(-((x - c.μ) / c.σ)^2 / 2) / (c.σ * sqrt(2π)) for c in comps)\n\nN = 2000\nx = [rand() < comps[1].w ? comps[1].μ + comps[1].σ * randn() :\n                           comps[2].μ + comps[2].σ * randn() for _ in 1:N]\n\nki = kappa_interval(x)                          # a principled scale + range (see below)\nd = PenalizedDensityEstimate(x; κ = ki.κ)       # fit at the half-entropy scale\nnothing # hide\n\nd is a callable density: d(x) returns Q(x). Plotting the estimate against the truth — here at the cross-validated scale (see select_kappa_cv below) and the half-entropy scale kappa (the plotting code uses Makie, which is not a dependency of the package):\n\nusing CairoMakie\nκcv = select_kappa_cv(x)\nd_cv = PenalizedDensityEstimate(x; κ = κcv)\ng = range(-4.5, 7.5; length = 800)\nlines(g, truepdf.(g); linestyle = :dash, label = \"true density\")\nlines!(g, d_cv.(g); label = \"κ = $(round(κcv, digits=1)) (cross-validated)\")\nlines!(g, d.(g); label = \"κ = $(round(ki.κ, digits=1)) (half-entropy)\")\naxislegend()\n\n(Image: Two-Gaussian mixture recovered with a single κ)\n\nBoth scales recover both peaks: the cross-validated kappa is visibly smoother, the half-entropy kappa sharper, but each resolves the narrow and the broad component at once. This is the key point: κ is a resolution scale, not a component width. Its reciprocal 1kappa is smaller than either component's sigma, so the estimator resolves features of any larger width; the local width of Q is set by the data, not by kappa. The two scales come from kappa_interval and select_kappa_cv, both introduced next.","title":"A worked example"},{"category":"section","location":"tutorial/#Choosing-the-smoothing-scale","page":"Tutorial","text":"To pick kappa automatically, kappa_interval returns a principled scale with a plausible range. Its basis is that the reduced action g(kappa) = S(kappa) + Wlnkappa (W = total count) rises monotonically between two exact limits — W2 as kappato0 (all points merge into one lump) and W2 + W H as kappatoinfty (the N points become isolated), where H is the Shannon entropy of the data. The normalized quantity h(kappa)in01 is thus the fraction of the data's entropy that kappa resolves, and its half-point is the returned scale:\n\nki = kappa_interval(x)          # (; κ, lo, hi): the half-entropy scale and a band\n(κ = round(ki.κ, digits=1), lo = round(ki.lo, digits=1), hi = round(ki.hi, digits=1))\n\nThe picture below makes this concrete. The reduced action (red) climbs from its kappato0 floor W2 (nothing resolved, h=0) to its kappatoinfty ceiling W2 + W H (every point resolved, h=1); both dashed limits are exact and depend only on the counts. The returned scale is where the curve is halfway up — half the entropy resolved — and the shaded band is the plausible range:\n\n(Image: Reduced action rising between its two entropy limits)\n\nselect_kappa returns a related but distinct scale: the point of minimum sensitivity, where |dS/d ln κ| is smallest. Its derivative is computed analytically, so the result is free of the noise that finite-differencing the action curve would introduce. The two criteria generally select different scales. Both resolve information in the data rather than minimizing error, and on smooth densities they tend to over-resolve.\n\nWhen the goal is instead minimum mean integrated squared error (MISE), use select_kappa_cv, which minimizes a least-squares cross-validation score:\n\nκ_cv = select_kappa_cv(x)       # error-optimal scale, usually finer than select_kappa\n\nIts score is an unbiased estimate of the integrated squared error ∫(Q̂ − Q)², evaluated analytically — the ∫Q̂² term in closed form and each leave-one-out density from a first-order expansion, so no point-by-point refitting is needed. Cross-validation assumes a continuous underlying density; on heavily tied or coarsely rounded data it undersmooths, and the action-based selectors are the better choice there.","title":"Choosing the smoothing scale"},{"category":"section","location":"tutorial/#Goodness-of-fit","page":"Tutorial","text":"Because the estimate is a genuine likelihood fit, you can ask how well a specific model distribution describes the data. Using the half-entropy fit d, chisq is a robust, binning-free chi^2 (the squared Hellinger distance between a trial density and the data):\n\nwrong(x) = exp(-((x - 0.5) / 2.5)^2 / 2) / (2.5 * sqrt(2π))   # a single broad Gaussian\n\n(correct = round(chisq(d, truepdf); digits = 1),\n incorrect = round(chisq(d, wrong); digits = 1))\n\nThe true mixture yields a far smaller chi^2 than the mismatched single Gaussian. pvalue turns the statistic into a significance under the known large-N reference distribution:\n\n(p_correct = round(pvalue(d, truepdf); digits = 3),\n p_incorrect = round(pvalue(d, wrong); digits = 6))\n\nThe single Gaussian is decisively rejected; the true mixture is not. (The large-N p-value is conservative for an accepted model, so the headline discriminator is the chi^2 value itself.) expected_chisq gives the mean of the reference distribution (about 07 per effective bin), and chisq_pdf / chisq_ccdf give its full density and upper-tail probability — an inverse-Gaussian law.","title":"Goodness of fit"}]
}
