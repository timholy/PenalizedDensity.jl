var documenterSearchIndex = {"docs":
[{"category":"section","location":"reference/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"type","location":"reference/#PenalizedDensity.AdaptiveScale","page":"API reference","text":"AdaptiveScale(c, α, pilot)\n\nA spatially varying smoothing scale of the plug-in form\n\nκ(x) = c · (p̂(x) / ḡ)^α,\n\nwhere p̂ is the pilot density estimate and ḡ the geometric mean of p̂ over the sample it was fitted to. select_kappa_adaptive constructs one by choosing c and α; the result is callable, and is passed straight to DensityEstimate as the smoothing scale.\n\nDividing by ḡ puts c on the same footing as a constant scale: where p̂ equals its geometric mean, κ(x) = c. The exponent α > 0 sets how strongly the scale follows the density — larger α resolves the peaks more finely and smooths the tails more heavily.\n\nThe scale is floored at 1e-6 c, which intercepts underflow of (p̂/ḡ)^α at points where the pilot density is negligible; the floor sits far below any scale the rule would otherwise choose, so it never shapes the fit.\n\n\n\n\n\n","title":"PenalizedDensity.AdaptiveScale"},{"category":"type","location":"reference/#PenalizedDensity.ChisqReference","page":"API reference","text":"ChisqReference\n\nPrecomputed reference distribution of the goodness-of-fit statistic chisq for one fit, in the exact finite-N theory (Holy 1997, Eqs. 16–18). The statistic is a quadratic form in the Gaussian fluctuation field, so its law is a generalized chi-squared; this object stores the O(N) data — a symmetric tridiagonal matrix and a rank-one constraint vector — that its density and tail probabilities are computed from.\n\nBuild one with chisq_reference and reuse it across many evaluations of chisq_ccdf, chisq_pdf, and pvalue; its exact mean is expected_chisq(ref).\n\n\n\n\n\n","title":"PenalizedDensity.ChisqReference"},{"category":"type","location":"reference/#PenalizedDensity.DensityEstimate","page":"API reference","text":"DensityEstimate(x::AbstractVector{T}, κ; support=(-Inf, Inf), rtol=cbrt(eps(T)))\n\nEstimate a continuous one-dimensional probability density from sample points x, using the scalar-field method of Holy, Phys. Rev. Lett. 79, 3545 (1997).\n\nκ is either a positive number, giving one scale everywhere, or a callable κ(x) returning the scale local to x; larger κ gives a rougher estimate. See select_kappa_kl for choosing it automatically (the recommended default; select_kappa_cv, select_kappa_ms, and kappa_interval are alternatives).\n\nsupport = (a, b) fits the density on a finite domain instead of all of ℝ; either end may be -Inf/Inf for a one-sided or fully unbounded fit (the default). The density Q is exactly zero outside [a, b], cdf reaches exactly 0 at a and 1 at b, and every data point must lie in [a, b] (checked at fit time; a violation, or a ≥ b, throws a DomainError).\n\nRepeated points, and points closer than rtol / κ(x) (i.e. within a fraction rtol of the local smoothing length), are merged into one node carrying the count as its integer weight, so weighted data is handled naturally. Without merging, the resulting tridiagonal system can be nearly singular.\n\nThe returned object is callable: d(x) evaluates the density Q(x) at any real x, and it can be broadcast over arrays. Use amplitude for ψ(x).\n\nPassing κ as a keyword, DensityEstimate(x; κ), is deprecated in favor of the positional form.\n\nExamples\n\njulia> d = DensityEstimate([-1.0, 0.0, 0.0, 1.0], 1.0);\n\njulia> d(0.0) > d(2.0)   # denser near the data\ntrue\n\njulia> round(chisq(d, d); digits=8)   # a distribution fits itself perfectly\n0.0\n\njulia> a = DensityEstimate([-1.0, 0.0, 0.0, 1.0], x -> 1 + exp(-x^2));  # sharper near 0\n\njulia> a.κ                                # one rate per inter-node interval\n2-element Vector{Float64}:\n 1.778800783071405\n 1.778800783071405\n\njulia> u = DensityEstimate(range(0.05, 0.95; length=50), 10.0; support=(0.0, 1.0));\n\njulia> u(-0.1), u(1.1)              # zero outside the support\n(0.0, 0.0)\n\njulia> cdf(u, 0.0), cdf(u, 1.0)     # cdf hits 0 and 1 exactly at the walls\n(0.0, 1.0)\n\nExtended help\n\nThe density is written as Q(x) = ψ(x)^2, where the amplitude ψ minimizes the action\n\nS[ψ] = ∫ (λ/κ(x)²) (ψ')² dx - 2 Σᵢ ln ψ(xᵢ)\n\nsubject to ∫ ψ² dx = 1, with λ the normalization multiplier. The smoothing scale κ sets the width of each point's contribution, and the penalty weight 1/κ(x)² on (ψ')² is what keeps the pressure to normalize spatially uniform.\n\nA callable κ(x) is evaluated at the midpoint of each inter-node interval, and at the outermost nodes for the two tails, so the fit resolves a piecewise-constant scale: d.κ[k] is the rate on (d.x[k], d.x[k+1]), and d.κL, d.κR the tail rates. Making κ large where the density is high and small where it is low buys resolution where the data can pay for it.\n\nBetween sorted data points ψ solves ψ'' = κ² ψ, i.e. it is a sum of rising and falling exponentials, and decays as e^{-κ|x|} in the tails. The nodal amplitudes ψ(xᵢ) satisfy a symmetric tridiagonal system whose solution is the minimizer of a strictly convex potential; normalization is then a rescaling.\n\nAt a finite support endpoint the density is left free rather than pinned to zero (a natural, or Neumann, boundary condition: ψ'(a) = 0) — the wall changes only the outermost interval on that side, replacing its exponential tail with a cosh arc pinned flat at the wall, so a discontinuous or divergent edge (a \"jump edge\") is representable directly rather than approximated by a fast-decaying tail.\n\nThe goodness-of-fit machinery (chisq_reference and everything built on it) supports a varying κ exactly as it does a constant one, and a finite support exactly as it does the unbounded line.\n\n\n\n\n\n","title":"PenalizedDensity.DensityEstimate"},{"category":"method","location":"reference/#PenalizedDensity._solve_amplitude-Union{Tuple{T}, Tuple{LinearAlgebra.SymTridiagonal{T, V} where V<:AbstractVector{T}, Vector{T}}} where T<:AbstractFloat","page":"API reference","text":"_solve_amplitude(M, w)    -> ψ\n_solve_amplitude(x, w, κ) -> ψ\n\nMinimize the strictly convex potential F(ψ) = ½ ψ'Mψ - Σ wᵢ ln ψᵢ over ψ > 0 by a damped Newton iteration with an SPD tridiagonal Hessian. The minimizer solves Mψ = w ./ ψ, i.e. the field equation at unit multiplier; the caller rescales it to impose normalization.\n\nEach step factorizes the tridiagonal Hessian in place (ldlt!/ldiv!) and backtracks along the Newton direction to keep ψ > 0 with Armijo decrease. Iteration stops when the Newton decrement λ² = ∇FᵀΔ drops below a relative tolerance, or when the line search can no longer decrease F.\n\n\n\n\n\n","title":"PenalizedDensity._solve_amplitude"},{"category":"method","location":"reference/#PenalizedDensity._sorted_sample-Union{Tuple{T}, Tuple{AbstractVector, Type{T}}} where T","page":"API reference","text":"_sorted_sample(x, T) -> xs::Vector{T}\n\nA sorted, one-based working copy of the sample x, whatever its axes. Every index the fit takes afterwards — into the sample, into a scale realized on it, into the merged nodes — is an index into this copy, and none of them escape, so the caller's axes have nothing to propagate to.\n\n\n\n\n\n","title":"PenalizedDensity._sorted_sample"},{"category":"method","location":"reference/#PenalizedDensity.action-Tuple{DensityEstimate}","page":"API reference","text":"action(d::DensityEstimate) -> S\n\nClassical action S[ψ_cl] = N - λ - Σᵢ wᵢ ln Q(xᵢ) (Eq. 10) of the fitted density, where N = Σ wᵢ. Used by select_kappa_ms.\n\n\n\n\n\n","title":"PenalizedDensity.action"},{"category":"method","location":"reference/#PenalizedDensity.amplitude-Tuple{DensityEstimate, Real}","page":"API reference","text":"amplitude(d::DensityEstimate, x)\n\nEvaluate the amplitude ψ(x) (so that the density is d(x) == ψ(x)^2) at real x, which may be a scalar or an array. Zero outside a finite support (see DensityEstimate); the fitted density is exactly zero there, not merely small.\n\n\n\n\n\n","title":"PenalizedDensity.amplitude"},{"category":"method","location":"reference/#PenalizedDensity.cdf-Tuple{DensityEstimate, Real}","page":"API reference","text":"cdf(d::DensityEstimate, x)\n\nCumulative distribution function of the fitted density, F(x) = ∫_a^x Q(t) dt with Q = ψ² and a the fit's left support endpoint (-Inf unless DensityEstimate was given a finite support), evaluated in closed form (no quadrature). F is nondecreasing with F(a) == 0 and F(b) == 1 exactly, and outside a finite [a, b] support is exactly 0 at or below a and exactly 1 at or above b.\n\nx may be a scalar or an array. Each call assembles the per-node cumulative masses at O(length(d.x)) cost; the array method assembles them once and shares them across all evaluations. See quantile for the inverse.\n\nExamples\n\njulia> d = DensityEstimate([0.0], 0.5);   # a Laplace density centered at 0\n\njulia> cdf(d, 0.0)\n0.5\n\njulia> quantile(d, 0.5)\n0.0\n\nExtended help\n\nThe tails (or, on a finite support, the two boundary segments) are pure exponentials or cosh arcs; on each inter-node interval ψ'' = κ²ψ makes ψ'² - κ²ψ² constant, which yields the exact antiderivative of the hyperbolic interpolant. The F(a) == 0/F(b) == 1 endpoints are exact because the few ulps of normalization roundoff in the fitted amplitudes are absorbed by a global rescaling.\n\n\n\n\n\n","title":"PenalizedDensity.cdf"},{"category":"method","location":"reference/#PenalizedDensity.chisq-Union{Tuple{T}, Tuple{DensityEstimate{T}, Any}} where T","page":"API reference","text":"chisq(d::DensityEstimate, Q) -> χ²\n\nGoodness-of-fit statistic between a trial density Q and the data underlying the fit d, the robust field-theoretic analogue of Pearson's χ² (Eqs. 13–14 of the paper):\n\nχ² = 4 Σᵢ wᵢ (√Q(xᵢ) / ψ_cl(xᵢ) - 1)²,\n\nsummed over the data nodes xᵢ with multiplicities wᵢ, where ψ_cl = √(d(·)) is the fitted amplitude. Q is any callable returning density values; it should be a normalized density (∫Q dx = 1). chisq(d, d) == 0. Small χ² means Q is close to the data in the (squared Hellinger) sense; see pvalue and chisq_ccdf for significance.\n\n\n\n\n\n","title":"PenalizedDensity.chisq"},{"category":"method","location":"reference/#PenalizedDensity.chisq_ccdf-Union{Tuple{T}, Tuple{ChisqReference{T}, Real}} where T","page":"API reference","text":"chisq_ccdf(d::DensityEstimate, z; method=:exact)   -> P(χ² ≥ z)\nchisq_ccdf(ref::ChisqReference, z; method=:exact)  -> P(χ² ≥ z)\n\nUpper-tail (survival) probability of the reference χ² distribution at z. Evaluated at an observed statistic it is a p-value; see pvalue.\n\nmethod=:exact (default) uses the finite-N generalized-χ² law via Imhof inversion of chisq_reference(d). method=:largeN uses the inverse-Gaussian (Wald) shape of the large-N limit (Eq. 26), parameterized by the exact mean expected_chisq; it is a closed form, far cheaper per call, and — like the exact law — defined at every scale. Pass a prebuilt ChisqReference to avoid reassembling it across calls.\n\n\n\n\n\n","title":"PenalizedDensity.chisq_ccdf"},{"category":"method","location":"reference/#PenalizedDensity.chisq_pdf-Union{Tuple{T}, Tuple{ChisqReference{T}, Real}} where T","page":"API reference","text":"chisq_pdf(d::DensityEstimate, z; method=:exact)   -> P(z)\nchisq_pdf(ref::ChisqReference, z; method=:exact)  -> P(z)\n\nDensity of the reference χ² distribution at z ≥ 0. method=:exact (default) is the finite-N generalized-χ² law from chisq_reference(d). method=:largeN is the inverse-Gaussian (Wald) density of the large-N limit (Eq. 26),\n\nP(z) = ⟨χ²⟩ / √(2π z³) · exp[⟨χ²⟩ - z/2 - ⟨χ²⟩²/(2z)],\n\nwith ⟨χ²⟩ = expected_chisq the exact mean: a closed form, defined at every scale. Pass a prebuilt ChisqReference to reuse it.\n\n\n\n\n\n","title":"PenalizedDensity.chisq_pdf"},{"category":"method","location":"reference/#PenalizedDensity.chisq_reference-Union{Tuple{DensityEstimate{T}}, Tuple{T}} where T","page":"API reference","text":"chisq_reference(d::DensityEstimate) -> ChisqReference\n\nAssemble the exact reference distribution of chisq for the fit d, following Holy 1997 (Eqs. 16–18). Costs O(N); reuse the result across many calls to chisq_ccdf/chisq_pdf/pvalue rather than rebuilding it. A spatially varying κ and a finite support (see DensityEstimate) are both supported, and the law stays exact and O(N) in either case.\n\nExtended help\n\nWith a spatially varying κ the nodal precision of the fluctuation field is 2λ times the same tridiagonal operator the fit assembles, whatever the scale. With a finite support the fluctuation field's natural (Neumann) boundary condition makes M̂ the Dirichlet-to-Neumann map of the boundary segments as well as the interior, and the same identity G₀⁻¹ = 2λM̂ holds with M̂ the bounded operator the fit already assembles.\n\n\n\n\n\n","title":"PenalizedDensity.chisq_reference"},{"category":"method","location":"reference/#PenalizedDensity.entropy-Tuple{DensityEstimate}","page":"API reference","text":"entropy(d::DensityEstimate) -> Ĥ\n\nPlug-in estimate of the differential entropy H(Q) = -∫ Q ln Q dx of the fitted density,\n\nĤ = -(1/W) Σᵢ wᵢ ln Q(xᵢ) = -(2/W) Σᵢ wᵢ ln ψ(xᵢ),\n\nwhere W = Σᵢ wᵢ. Evaluating ln Q at the data rather than integrating it exactly avoids a second quadrature pass and is consistent (Ĥ → H(Q) as the sample grows), but it is biased at small W: for the one-point fit (a Laplace density with rate 2κ), Ĥ = -ln κ against the exact H = 1 - ln κ. See negentropy.\n\n\n\n\n\n","title":"PenalizedDensity.entropy"},{"category":"method","location":"reference/#PenalizedDensity.entropy-Union{Tuple{T}, Tuple{DensityEstimate{T}, AbstractVector}} where T","page":"API reference","text":"entropy(d::DensityEstimate, xeval::AbstractVector) -> Ĥ\n\nHeld-out plug-in estimate of the differential entropy of the fitted density d, scored at points xeval that did not enter the fit d:\n\nĤ = -(1/M) Σⱼ ln Q̂(xevalⱼ) = -(2/M) Σⱼ ln ψ(xevalⱼ),   M = length(xeval).\n\nUnlike the one-argument entropy(d), which evaluates ln Q̂ at the fit's own nodes, this evaluates it at independent points and so does not reward collapsing the density onto the fit sample. It is the entropy term of negentropy(d, xeval).\n\n\n\n\n\n","title":"PenalizedDensity.entropy"},{"category":"method","location":"reference/#PenalizedDensity.expected_chisq-Tuple{DensityEstimate}","page":"API reference","text":"expected_chisq(d::DensityEstimate) -> ⟨χ²⟩\nexpected_chisq(ref::ChisqReference) -> ⟨χ²⟩\n\nMean of the reference distribution of chisq, in the exact finite-N theory (Holy 1997, Eqs. 16–18). Defined at any scale, constant or spatially varying.\n\nGiven a DensityEstimate, chisq_reference is assembled internally; to draw several quantities from one fit, build the reference once and pass it here and to chisq_ccdf/chisq_pdf/pvalue rather than reassembling it.\n\n\n\n\n\n","title":"PenalizedDensity.expected_chisq"},{"category":"method","location":"reference/#PenalizedDensity.kappa_interval-Tuple{AbstractVector{<:Real}}","page":"API reference","text":"kappa_interval(x; level=0.2, rtol=1e-6) -> (; κ, lo, hi)\n\nPrincipled smoothing-scale selection returning a point value and an interval of plausible scales. κ is the half-entropy scale — the h = 1/2 point of the entropy fraction h(κ) defined below — and lo, hi bracket h ∈ [(1-level)/2, (1+level)/2], so the default level=0.2 spans h ∈ [0.4, 0.6]. Requires at least two distinct points.\n\nThis entropy criterion is distinct from the minimum-sensitivity scale of select_kappa_ms; one advantage of this function is that it doesn't require computing a noisy numerical derivative.\n\nThis selector takes no support keyword: the exact κ → 0/κ → ∞ entropy limits it relies on are derived for the unbounded line, so it always fits (and returns a scale for) the unbounded problem.\n\nExtended help\n\nAs κ sweeps from 0 to ∞ the classical action's reduced form g(κ) = S(κ) + W ln κ (with W = Σ wᵢ the total count) rises monotonically between two exact limits: g → W/2 as κ → 0 (all points merge into one lump) and g → W/2 + W H as κ → ∞ (the N points become isolated), where H = -Σᵢ (wᵢ/W) ln(wᵢ/W) is the Shannon entropy of the multiplicities (ln N for distinct points). The normalized quantity\n\nh(κ) = (g(κ) - W/2) / (W H) ∈ [0, 1]\n\nis therefore the fraction of the data's entropy that scale κ resolves, and its half-point h = 1/2 is returned as κ.\n\n\n\n\n\n","title":"PenalizedDensity.kappa_interval"},{"category":"method","location":"reference/#PenalizedDensity.logdensity_eval_gradient-Tuple{DensityEstimate, Real}","page":"API reference","text":"logdensity_eval_gradient(d::DensityEstimate, y::Real) -> ∂ln Q̂(y)/∂y\n\nDerivative of the log fitted density with respect to the evaluation coordinate,\n\n∂ ln Q̂(y) / ∂y = 2 ψ'(y) / ψ(y),\n\nin closed form: ψ, ψ' are the hyperbolic interpolant and its derivative on the interval enclosing y (and ±κ ψ in the tails), so the cost is O(1) per point with no linear solve. See logdensity_node_gradient for the sensitivity to the node positions instead. The log density has a kink at each node d.x[k] (ψ' jumps there), so at a node the value returned is the one-sided derivative approaching from within the enclosing interval.\n\n\n\n\n\n","title":"PenalizedDensity.logdensity_eval_gradient"},{"category":"method","location":"reference/#PenalizedDensity.logdensity_node_gradient-Union{Tuple{T}, Tuple{DensityEstimate{T, T}, AbstractVector}, Tuple{DensityEstimate{T, T}, AbstractVector, Any}} where T","page":"API reference","text":"logdensity_node_gradient(d::DensityEstimate, yeval::AbstractVector, weights=nothing) -> g\n\nGradient, with respect to the node positions d.x, of the weighted sum of log densities at the held-out points yeval:\n\ng[i] = ∂/∂x_i  Σⱼ weightsⱼ · ln Q̂(yevalⱼ),\n\ncomputed by the implicit-function adjoint of the fit. Moving a node perturbs the fitted amplitude everywhere through the field equation M ψ = w ⊘ ψ; differentiating it reuses the fit's factored Hessian ∇²F = M + diag(wᵢ/ψᵢ²), and the adjoint aggregates the whole evaluation batch into a single extra tridiagonal solve. The cost is therefore O(N + M) for N nodes and M evaluation points, not one solve per point.\n\nweights defaults to all ones (the plain sum Σⱼ ln Q̂(yevalⱼ)); pass fill(1/M, M) for the mean. yeval should be disjoint from the fit's nodes — an evaluation point coinciding with a node sits on the log density's kink, where the gradient is one-sided. The returned g is indexed like d.x; a caller that moved the nodes through some parameter contracts g with the node-position Jacobian to obtain the parameter gradient. See logdensity_eval_gradient for the sensitivity to the evaluation point instead.\n\n\n\n\n\n","title":"PenalizedDensity.logdensity_node_gradient"},{"category":"method","location":"reference/#PenalizedDensity.negentropy-Union{Tuple{DensityEstimate{T}}, Tuple{T}} where T","page":"API reference","text":"negentropy(d::DensityEstimate) -> J\n\nNegentropy of the fitted density: the entropy deficit relative to the Gaussian with the same mean and variance,\n\nJ = ½ ln(2π e σ²) - Ĥ,\n\nwhere σ² is the fit's variance (computed analytically from the nodal amplitudes, the same per-interval hyperbolic closed form as action's normalization) and Ĥ is entropy(d). J ≥ 0 in the large-sample limit (the Gaussian maximizes entropy at fixed variance), but the plug-in Ĥ can push a finite-sample estimate slightly off zero even when the underlying density is Gaussian. J is invariant under x ↦ a·x + b (with κ ↦ κ/|a| for the corresponding fit, per the package's scale equivariance): both terms shift by ln|a| under the rescaling and cancel.\n\n\n\n\n\n","title":"PenalizedDensity.negentropy"},{"category":"method","location":"reference/#PenalizedDensity.negentropy-Union{Tuple{T}, Tuple{DensityEstimate{T}, AbstractVector}} where T","page":"API reference","text":"negentropy(d::DensityEstimate, xeval::AbstractVector) -> Ĵ\n\nHeld-out negentropy of the fitted density d, scored at independent points xeval:\n\nĴ = ½ ln(2π e s²) - Ĥ,\n\nwhere Ĥ = entropy(d, xeval) and s² is the empirical (maximum-likelihood, i.e. population) variance of xeval. The Gaussian reference thus uses the evaluation batch's own moments, not the fitted density's; with that choice ½ ln(2π e s²) = -(1/M) Σⱼ ln 𝒩(xevalⱼ) is itself a held-out expectation, so Ĵ is the mean held-out log-likelihood ratio of the fit against its matched Gaussian. It is invariant under xeval ↦ a·xeval + b together with the κ ↦ κ/|a| rescaling of the fit (both terms shift by ln|a| and cancel).\n\n\n\n\n\n","title":"PenalizedDensity.negentropy"},{"category":"method","location":"reference/#PenalizedDensity.pvalue-Tuple{ChisqReference, Real}","page":"API reference","text":"pvalue(d::DensityEstimate, Q; method=:exact)    -> p\npvalue(ref::ChisqReference, χ²; method=:exact)  -> p\n\nSignificance of the fit of a trial density Q: the probability that the reference χ² distribution exceeds the observed chisq(d, Q), i.e. chisq_ccdf(d, chisq(d, Q)).\n\nmethod is as in chisq_ccdf. To test several trial densities against one fit, build the reference once with chisq_reference and call pvalue(ref, chisq(d, Q)).\n\n\n\n\n\n","title":"PenalizedDensity.pvalue"},{"category":"method","location":"reference/#PenalizedDensity.select_kappa_adaptive-Tuple{AbstractVector{<:Real}}","page":"API reference","text":"select_kappa_adaptive(x; alphas=(0.25, 0.5, 0.75, 1.0), pilot_selector=select_kappa_kl,\n    rtol=cbrt(eps(T)), support=(-Inf, Inf)) -> κ\n\nChoose a spatially varying smoothing scale by Kullback–Leibler cross-validation, and return it ready to pass to DensityEstimate.\n\nReturns an AdaptiveScale when some α in alphas beats the constant scale, and the constant scale itself (a number, so the fit takes the constant-κ path and its goodness-of-fit machinery stays available) otherwise. The constant scale always competes, on the same score, so the returned scale is adaptive only if adaptivity wins. Selection costs a small multiple of one select_kappa_kl call; shorten alphas to trade capture for speed.\n\nThe alphas must be positive: α = 0 is the constant scale, which is always in the comparison. They are searched in increasing order, whatever order they are given in. pilot_selector sets the constant scale of the pilot density the family is built from, and may be any callable returning a positive scale from the sample. rtol is the node-merging tolerance, as a fraction of the local smoothing length, matching DensityEstimate's.\n\nsupport = (a, b) (default (-Inf, Inf)) fits the pilot density and cross-validates every candidate scale on a finite domain, as DensityEstimate's support does; it is a fixed hyperparameter of the search, not itself selected, and is held fixed across every candidate α/c. Data outside [a, b], or a ≥ b, throws a DomainError. Composing this selector with select_support — which chooses support (and a constant κ) by the same cross-validation score — is two documented steps, not one entry point: call select_support first, then pass its support here, then fit DensityEstimate(x, κ; support) with the scale this returns.\n\nExamples\n\njulia> x = -log.(1 .- (0.5:999.5) ./ 1000);   # exponential: a jump at the left edge\n\njulia> κ = select_kappa_adaptive(x);          # adaptivity wins here\n\njulia> κ.α                                    # the selected exponent\n0.5\n\njulia> d = DensityEstimate(x, κ);\n\njulia> extrema(d.κ)[2] / extrema(d.κ)[1] > 100   # far finer at the edge than in the tail\ntrue\n\njulia> select_kappa_adaptive(range(0, 1; length=1000)) isa Real   # uniform: nothing to buy\ntrue\n\nExtended help\n\nA single scale must trade resolution in the bulk against noise in the tails. Letting κ follow the density lifts that trade-off, and buys the most where a constant scale is limited not by noise but by the density's own irregularity: a divergent or discontinuous edge, a kink, or heavy tails. On smooth densities there is nothing to buy, and this selector says so — it returns a plain number, the constant scale, whenever adaptivity does not earn its keep by the same cross-validation score that chose it.\n\nThe rule is a plug-in: fit a pilot density p̂ at the constant scale pilot_selector(x) (by default select_kappa_kl), then consider the family\n\nκ(x; c, α) = c · (p̂(x) / ḡ)^α,     ḡ = geometric mean of p̂ over the sample\n\n(an AdaptiveScale). For each exponent α in alphas, c is chosen by golden-section search on the leave-one-out score KLCV(κ) = -(1/N) Σᵢ wᵢ ln Q̂₋ᵢ(xᵢ) generalized to a varying scale — the same criterion select_kappa_kl minimizes, and, like it, evaluated in closed form and O(N), with no refitting. The constant scale competes as the α = 0 member of the same family and on the same score.\n\npilot_selector is a scale-selection method, and is called on the sample alone with no notion of support; the pilot density it scales is what is fitted on support. So a selector with no notion of a boundary, like select_kappa_ms, remains usable as pilot_selector on a bounded domain. Because support is fixed throughout the α search, composing with select_support re-runs that search on each boundary arm's own domain, so α gets to respond to whatever regularity a boundary already bought, rather than being reused from an unbounded selection that saw a different edge.\n\n\n\n\n\n","title":"PenalizedDensity.select_kappa_adaptive"},{"category":"method","location":"reference/#PenalizedDensity.select_kappa_cv-Tuple{AbstractVector{<:Real}}","page":"API reference","text":"select_kappa_cv(x; κs=<data-scaled grid>, rtol=1e-6, support=(-Inf, Inf)) -> κ\n\nChoose the smoothing scale by least-squares cross-validation: return the κ minimizing\n\nLSCV(κ) = ∫ Q̂_κ(x)² dx - (2/N) Σᵢ Q̂_{κ,-i}(xᵢ),\n\nan unbiased estimate — up to the κ-independent ∫Q² — of the integrated squared error ∫(Q̂_κ - Q)², where Q̂_{κ,-i} is the density fitted with the i-th point left out. Its minimizer therefore targets minimum mean integrated squared error (MISE). This generally selects a finer scale than select_kappa_ms (minimum sensitivity) and kappa_interval (half-entropy), which resolve information rather than squared error and tend to over-resolve smooth densities.\n\nsupport = (a, b) (default (-Inf, Inf)) fits and cross-validates on a finite domain, as DensityEstimate's support does; it is a fixed hyperparameter of the search, not itself selected, and is held fixed across every candidate κ. Data outside [a, b], or a ≥ b, throws a DomainError. κs must be sorted and positive, with at least three values to bracket the minimum, and defaults to a geometric range scaled to the data's extent.\n\nCross-validation assumes the data are draws from a continuous density. Heavily tied or coarsely rounded data instead resemble a discrete distribution, for which LSCV decreases without bound as κ → ∞ (finer scales keep resolving the atoms); select_kappa_cv then returns a large κ. Prefer select_kappa_ms or kappa_interval, which stay bounded, in that regime.\n\nExtended help\n\nBoth terms are evaluated analytically in O(N): ∫Q̂² in closed form over the exponential segments, and each leave-one-out density Q̂_{-i}(xᵢ) from a first-order expansion of the fit in the dropped point's weight, so no per-point refitting is needed. The score is minimized by a golden-section search over ln κ, bracketed by the grid κs.\n\n\n\n\n\n","title":"PenalizedDensity.select_kappa_cv"},{"category":"method","location":"reference/#PenalizedDensity.select_kappa_kl-Tuple{AbstractVector{<:Real}}","page":"API reference","text":"select_kappa_kl(x; κs=<data-scaled grid>, rtol=1e-6, support=(-Inf, Inf)) -> κ\n\nChoose the smoothing scale by Kullback–Leibler (likelihood) cross-validation: return the κ minimizing the mean negative leave-one-out log-likelihood\n\nKLCV(κ) = -(1/N) Σᵢ wᵢ ln Q̂_{κ,-i}(xᵢ),\n\nwhere Q̂_{κ,-i} is the density fitted with the i-th point left out. This is the recommended default selector: on a range of test densities it tracks the error-optimal scale most closely of the four (see benchmarks/), and it is the cheapest of the cross-validation scores to evaluate. Like select_kappa_cv it generally selects a finer scale than select_kappa_ms and kappa_interval, which resolve information rather than divergence.\n\nsupport = (a, b) (default (-Inf, Inf)) fits and cross-validates on a finite domain, as DensityEstimate's support does; it is a fixed hyperparameter of the search, not itself selected, and is held fixed across every candidate κ. Data outside [a, b], or a ≥ b, throws a DomainError. κs must be sorted and positive, with at least three values to bracket the minimum, and defaults to a geometric range scaled to the data's extent.\n\nCross-validation assumes the data are draws from a continuous density. Heavily tied or coarsely rounded data instead resemble a discrete distribution, for which the leave-one-out log-likelihood increases without bound as κ → ∞ (leaving out one of many coincident copies barely perturbs the fit); select_kappa_kl then returns a large κ. Prefer select_kappa_ms or kappa_interval, which stay bounded, in that regime.\n\nExtended help\n\nKLCV estimates, up to a κ-independent constant, the Kullback–Leibler divergence KL(Q ‖ Q̂_κ); minimizing it is maximum-likelihood cross-validation. It is the criterion native to the estimator, whose action -Σ ln Q̂(xᵢ) is itself the (in-sample) log-likelihood, and to leading order it selects the same error-optimal scale as select_kappa_cv while being cheaper: the ∫Q̂² roughness term is not needed.\n\nEach leave-one-out density Q̂_{-i}(xᵢ) comes from a first-order expansion of the fit in the dropped point's weight, so no per-point refitting is needed and the score costs O(N). The score is minimized by a golden-section search over ln κ, bracketed by the grid κs.\n\n\n\n\n\n","title":"PenalizedDensity.select_kappa_kl"},{"category":"method","location":"reference/#PenalizedDensity.select_kappa_ms-Tuple{AbstractVector{<:Real}}","page":"API reference","text":"select_kappa_ms(x; κs=<data-scaled grid>, rtol=1e-6) -> κ\n\nChoose the smoothing scale by the principle of minimum sensitivity: return the κ at which the classical action action S is least sensitive to the scale, i.e. |dS/d ln κ| is smallest (Fig. 1 of the paper). κs must be sorted and positive, with at least three values to bracket the minimum, and defaults to a geometric range scaled to the data's extent.\n\nThis and the entropy-based kappa_interval both resolve information and over-resolve smooth densities; to target estimation error instead, prefer select_kappa_kl (the recommended default) or select_kappa_cv. The information-resolving scales here and in kappa_interval are the better choice only for heavily tied or discrete data, where the cross-validation scores are unbounded.\n\nThis selector takes no support keyword: the entropy asymptotics behind minimum sensitivity are derived for the unbounded line and do not generalize to a finite domain, so it always fits (and returns a scale for) the unbounded problem.\n\nExtended help\n\nThe derivative dS/d ln κ is evaluated analytically and minimized over κ by a golden-section search, bracketed by the grid κs. This is a principled convention rather than a unique optimum: S has no exact stationary point in κ, so the flattest point depends on measuring sensitivity in ln κ.\n\n\n\n\n\n","title":"PenalizedDensity.select_kappa_ms"},{"category":"method","location":"reference/#PenalizedDensity.select_support-Tuple{AbstractVector{<:Real}}","page":"API reference","text":"select_support(x; kappa=select_kappa_kl, κs=<data-scaled grid>, rtol=1e-6) -> (; κ, support)\n\nChoose a domain support = (a, b) — either side possibly infinite — together with the smoothing scale κ, jointly, by the same Kullback–Leibler cross-validation score select_kappa_kl minimizes. Pass the result straight to DensityEstimate:\n\nr = select_support(x)\nd = DensityEstimate(x, r.κ; support = r.support)\n\nA boundary is imposed on a side only when it wins that cross-validation, never assumed from the fact that one side of the data has an edge; a side that does not win stays ±Inf. A finite boundary is always placed outward of the extreme data point on its side, and never closer to it than five times the mean spacing of the data near that edge. When neither side wins, the support is (-Inf, Inf) and the returned κ equals kappa(x; κs, rtol) exactly — a family with nothing to gain from a boundary gets the standalone selection itself, not merely something close to it.\n\nkappa (default select_kappa_kl) must share select_kappa_kl's (x; κs, rtol, support) interface, as select_kappa_cv does. κs and rtol are passed through to it, and set the golden-section bracket and the node-merging tolerance (a fraction of the local smoothing length) throughout the search.\n\nExamples\n\njulia> x = -log.(1 .- (0.5:499.5) ./ 500);   # exponential draw: a jump edge at the left\n\njulia> r = select_support(x);\n\njulia> r.support[1] <= minimum(x) && r.support[2] == Inf   # never inward of the data\ntrue\n\njulia> d = DensityEstimate(x, r.κ; support = r.support);\n\njulia> d.lo == r.support[1] && d.hi == r.support[2]\ntrue\n\nExtended help\n\nEach side is searched independently and sequentially — the left boundary first (with the right side unbounded), then the right boundary against the left side's winner — and on each side the unbounded (±Inf) candidate always competes: that side gets a finite boundary only if the best finite candidate's KLCV beats the score of leaving it unbounded by more than a small margin (screening out golden-section/floating-point noise, not a real effect size). A wall is not always safe to add: placed too far past the data it can raise the KLCV score rather than lower it (a flat field props mass into an empty margin where a decaying tail would not).\n\nA finite candidate on one side is a gap Δ > 0, the distance outward from the extreme data point on that side (a = x₁ - Δ on the left, b = x_N + Δ on the right), searched by golden-section on ln Δ over a bracket of [5, 100] times the mean spacing of the ten data points nearest that edge (extensible further outward, never inward). The lower end is a hard floor, not merely a starting guess: closer than a few edge spacings, a natural boundary reflects the nearest interior points back onto the extreme point and inflates its leave-one-out likelihood on any sample, edge or not, so gaps tighter than the floor are excluded rather than searched (this is a property of the reflecting boundary condition itself — confirmed against a brute-force leave-one-out refit — not a search artifact). κ is re-selected at every gap candidate rather than held fixed, because the two are coupled at a hard edge (the optimal κ can move to a fraction of its unbounded value once a wall is added); candidates are searched from the widest gap to the narrowest, and each candidate's κ search is warm-started in a narrow window about the previous candidate's optimum rather than repeating a full search from scratch, since κ* moves continuously with the gap.\n\nkappa is consulted at two points only: once at the start, to seed the unbounded arm's competing score and the first (and widest) gap candidate's κ warm start; and once at the end, to refine κ at the winning support over the full κs bracket a standalone call would use (the chained inner searches above use a narrower window, for speed). When neither side wins, no refinement call is made and the returned κ is that first call. The gap-path searches themselves score every candidate directly by the KLCV score select_kappa_kl uses, not by calling kappa per candidate.\n\n\n\n\n\n","title":"PenalizedDensity.select_support"},{"category":"method","location":"reference/#Statistics.quantile-Tuple{DensityEstimate, Real}","page":"API reference","text":"quantile(d::DensityEstimate, q)\n\nQuantile function of the fitted density, the inverse of cdf: cdf(d, quantile(d, q)) ≈ q for q ∈ [0, 1], with quantile(d, 0) == a and quantile(d, 1) == b (the fit's support endpoints, -Inf/Inf unless DensityEstimate was given a finite support); q outside [0, 1] (including NaN) throws a DomainError. q may be a scalar or an array; the array method assembles the per-node cumulative masses once and shares them across all evaluations. Extends Statistics.quantile.\n\nIn an unbounded tail the inversion is in closed form; on interior intervals, and on a finite boundary segment (transcendental there), a Newton iteration on the closed-form CDF, bracketed by the enclosing nodes or by the wall and the outermost node, converges to floating-point accuracy. The right side is solved through 1 - q, so upper quantiles lose no more precision than q itself carries.\n\n\n\n\n\n","title":"Statistics.quantile"},{"category":"section","location":"#PenalizedDensity","page":"Home","text":"PenalizedDensity estimates a continuous one-dimensional probability density from sample points, without binning, using the penalized maximum-likelihood scalar-field method of\n\nT. E. Holy, \"Analysis of Data from Continuous Probability Distributions,\" Phys. Rev. Lett. 79, 3545 (1997).\n\nThe density is written Q(x) = psi(x)^2, and the amplitude psi maximizes the likelihood of the data subject to a smoothness penalty and normalization int Qdx = 1. Each data point contributes a peak of width sim 1kappa; the smoothing scale kappa trades resolution against smoothness, and may either be a single number or vary across the data, kappa(x), to resolve an irregular density where a single scale cannot. The domain itself may also be bounded, fitting a hard edge exactly rather than approximating it with a decaying tail. Sorting the data and writing psi as rising and falling exponentials between adjacent points reduces the estimator to a symmetric tridiagonal problem, solved by a convex Newton iteration in O(N) time with normalization imposed by a single rescaling.","title":"PenalizedDensity"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"PenalizedDensity\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using PenalizedDensity\n\nxs = [-2.1, -0.4, -0.4, 0.3, 1.2, 1.9]     # samples from an unknown distribution\n\nd = DensityEstimate(xs, 1.0)  # d is Q, and callable: d(x) returns the density estimate at x \n(d(0.0), d(2.0))\n\nThe result is callable and returns the estimated density; amplitude gives psi(x) = sqrtQ(x).\n\nThe Tutorial works through choosing the smoothing scale, testing a model against the data, and measuring the fit's entropy and negentropy; the API reference documents every function.","title":"Quick start"},{"category":"section","location":"tutorial/#Tutorial","page":"Tutorial","text":"This walkthrough fits a bimodal distribution, chooses the smoothing scale, and tests candidate models against the data.","title":"Tutorial"},{"category":"section","location":"tutorial/#A-worked-example","page":"Tutorial","text":"Consider a mixture of two Gaussians with equal weight, a narrow one (sigma=04) centered at x=-2 and a broad one (sigma=12) at x=3. First let's draw some samples from this distribution:\n\nusing Random\n\nRandom.seed!(42)\ncomps = [(w=0.5, μ=-2.0, σ=0.4), (w=0.5, μ=3.0, σ=1.2)]\ntruepdf(x) = sum(c.w * exp(-((x - c.μ) / c.σ)^2 / 2) / (c.σ * sqrt(2π)) for c in comps)\n\nN = 2000\nxs = [rand() < comps[1].w ? comps[1].μ + comps[1].σ * randn() :\n                            comps[2].μ + comps[2].σ * randn() for _ in 1:N]\nnothing # hide\n\nTo construct an estimate of the density from the points xs, we first select a smoothing length scale and then construct the density:\n\nusing PenalizedDensity\n\nκ = select_kappa_kl(xs)                 # recommended scale (KL cross-validation)\nd = DensityEstimate(xs, κ)              # fit at the recommended scale\nnothing # hide\n\nκ determines the amount of smoothing: the amplitude of the density estimate will decay over a length scale 1/κ. d is a callable density: d(x) returns the estimated density at a single point x. Let's plot this estimate (constructed using the recommended select_kappa_kl to compute the scale), along with the result for a different kappa estimator, the half-entropy as computed by kappa_interval: \n\nusing CairoMakie                              # for plotting\nki = kappa_interval(x)                        # alternative estimator for scale\nd_half = DensityEstimate(x, ki.κ)             # construct the alternative estimate\ng = range(-4.5, 7.5; length = 800)\nlines(g, truepdf.(g); linestyle = :dash, label = \"true density\")\nlines!(g, d.(g); label = \"κ = $(round(κ, digits=1)) (KL, recommended)\")\nlines!(g, d_half.(g); label = \"κ = $(round(ki.κ, digits=1)) (half-entropy)\")\naxislegend()\n\n(Image: Two-Gaussian mixture recovered with a single κ)\n\nBoth scales recover both peaks: the recommended KL kappa is visibly smoother, the half-entropy kappa sharper, but each resolves the narrow and the broad component at once. Note that κ is a resolution scale, not a component width. Its reciprocal 1kappa is smaller than either component's sigma, so the estimator resolves features of any larger width, which is set by the underlying distribution and the data obtained by sampling from it.","title":"A worked example"},{"category":"section","location":"tutorial/#Choosing-the-smoothing-scale","page":"Tutorial","text":"PenalizedDensity offers four automatic selectors. They split into two families: two that target estimation error (recommended for smooth data) and two that resolve information in the data (the right choice for heavily tied or discrete data). The recommended default is select_kappa_kl, which minimizes a likelihood (Kullback–Leibler) cross-validation score:\n\nκ_kl = select_kappa_kl(xs)       # recommended: error-optimal, likelihood cross-validation\n\nIts score is the leave-one-out log-likelihood -tfrac1Nsum_i ln hat Q_-i(x_i), where hat Q_-i(x_i) is an estimate at the point x_i formed from all points except x_i itself. This score is an estimate of mathrmKL(Q  hat Q_kappa) up to a constant; it is the criterion native to the estimator, whose action is itself a log-likelihood. A close relative, select_kappa_cv, instead minimizes a least-squares (MISE) cross-validation score:\n\nκ_cv = select_kappa_cv(xs)       # least-squares cross-validation (MISE)\n\nBoth are evaluated analytically — each leave-one-out density comes from a first-order expansion of the fit, so no point-by-point refitting is needed — and to leading order they select the same kappa propto N^15. Across a range of test densities select_kappa_kl tracks the error-optimal scale most closely (see benchmarks/) and is the cheaper of the two, which is why it is the default. Both assume a continuous underlying density; on heavily tied or coarsely rounded data their scores are unbounded as kappatoinfty, and the two information-resolving selectors below are the better choice.\n\nThe first information-resolving selector, kappa_interval, returns a principled scale with a plausible range. Its basis is that the reduced action g(kappa) = S(kappa) + Wlnkappa (W = total count) rises monotonically between two exact limits — W2 as kappato0 (all points merge into one lump) and W2 + W H as kappatoinfty (the N points become isolated), where H is the Shannon entropy of the data. The normalized quantity h(kappa)in01 is thus the fraction of the data's entropy that kappa resolves, and its half-point is the returned scale:\n\nki = kappa_interval(xs)          # (; κ, lo, hi): the half-entropy scale and a band\n(κ = round(ki.κ, digits=1), lo = round(ki.lo, digits=1), hi = round(ki.hi, digits=1))\n\nThe picture below makes this concrete. The reduced action (red) climbs from its kappato0 floor W2 (nothing resolved, h=0) to its kappatoinfty ceiling W2 + W H (every point resolved, h=1); both dashed limits are exact and depend only on the counts. The returned scale is where the curve is halfway up — half the entropy resolved — and the shaded band is the plausible range:\n\n(Image: Reduced action rising between its two entropy limits)\n\nThe second information-resolving selector, select_kappa_ms, returns a related but distinct scale: the point of minimum sensitivity, where |dS/d ln κ| is smallest. Its derivative is computed analytically, so the result is free of the noise that finite-differencing the action curve would introduce.\n\nκ_ms = select_kappa_ms(xs)       # minimum-sensitivity scale\n\nselect_kappa_ms and kappa_interval generally select different scales, but both resolve information in the data rather than minimizing error, and on smooth densities they tend to over-resolve — which is why the cross-validation selectors are recommended by default. Their value is on heavily tied or discrete data, where they stay bounded and the cross-validation scores do not.","title":"Choosing the smoothing scale"},{"category":"section","location":"tutorial/#Letting-the-scale-vary-across-the-data","page":"Tutorial","text":"Every selector above returns one kappa for the whole line, and a single scale has to compromise: fine enough to resolve the peaks, coarse enough to stay quiet in the tails. On a smooth density that compromise costs little. But when the density is irregular — a divergent or discontinuous edge, a kink, a heavy tail — a constant kappa is limited not by noise but by the density's own shape, and no choice of it is good everywhere.\n\nselect_kappa_adaptive lifts the compromise by letting the scale follow the density, kappa(x) large where the density is high, and small where it is low. In other words, the smoothing length scale will grow with the size of the expected gap between adjacent sampled points.\n\nAs an example, take a chi^2_1 sample, whose density diverges as x^-12 at the origin:\n\nusing PenalizedDensity, Random, Statistics\n\nRandom.seed!(7)\nz = randn(4000) .^ 2                  # χ²₁: the density diverges at x = 0\n\nκ_const = select_kappa_kl(z)          # one scale everywhere\nκ_var = select_kappa_adaptive(z)      # a scale that follows the density\n\nThe adaptive selector returns an AdaptiveScale — a callable kappa(x) — which DensityEstimate takes exactly where a number would go:\n\nd_const = DensityEstimate(z, κ_const)\nd_var = DensityEstimate(z, κ_var)\n\nBelow, the left panel shows true underlying density (dashed) and the two estimates d_const and d_var; the right panel shows how κ_const and κ_var depend on position.\n\n(Image: A varying κ against a divergent edge)\n\nNeither density estimate can track much below x approx 10^-3 (typically, fewer than one hundred points land within [0, 1e-3]), but the adaptive one tracks the power law to about tenfold-smaller x than the one with constant κ. The payoff is measurable on held-out data — the mean log-likelihood of a fresh sample, whose gap is the reduction in KL divergence, in nats per sample:\n\nztest = randn(4000) .^ 2\nloglik(d) = mean(log.(d.(ztest)))\n\n(constant = round(loglik(d_const); digits = 3),\n varying = round(loglik(d_var); digits = 3),\n gain = round(loglik(d_var) - loglik(d_const); digits = 3))\n\nConcretely, how is kappa(x) determined? The rule is a plug-in of the variable-bandwidth kind; Abramson's square-root law, Ann. Statist. 10, 1217 (1982), is the alpha = 12 member. A pilot fit hat p at the constant scale supplies the shape, and the scale is drawn from the family kappa(x) = c(hat p(x)bar g)^alpha (bar g the geometric mean of hat p over the sample). Both the overall scale c and the exponent alpha — how strongly kappa follows the density — are chosen by the same leave-one-out KL score that select_kappa_kl minimizes, generalized to a varying scale and still evaluated in closed form and in O(N):\n\n(c = round(κ_var.c; digits = 1), α = κ_var.α)   # the selected scale and exponent\n\nCrucially, the constant scale competes in that same comparison: it's the alpha = 0 member of the family, kappa(x) = c, so adaptivity is used only when it wins. When it does not, the selector says so by returning a plain number rather than an AdaptiveScale — as on uniform data, where kappa propto hat p^alpha has no contrast to exploit:\n\nselect_kappa_adaptive(rand(2000)) isa Real   # nothing to buy: the constant scale wins","title":"Letting the scale vary across the data"},{"category":"section","location":"tutorial/#Fitting-a-hard-edge","page":"Tutorial","text":"A varying scale sharpens resolution near a feature, but it still fits an amplitude built from exponential tails, which extend over all of mathbbR. When the true density has a genuine edge — zero on one side, nonzero on the other — no choice of kappa(x) reproduces that exactly: the fit always leaks a sliver of mass across the line. DensityEstimate's support keyword fixes this directly, by imposing a natural (Neumann) boundary condition at a finite endpoint rather than approximating it with decay; select_support chooses that endpoint from the data, the same way the other selectors choose kappa.\n\nAs an example, take a sample from the standard exponential distribution, whose density jumps from 0 to 1 at x=0:\n\nusing PenalizedDensity, Random, Statistics\n\nRandom.seed!(11)\nN = 1500\nx = -log.(1 .- rand(N))                 # exponential(1): a jump edge at x = 0\n\nκ_const = select_kappa_kl(x)\nd_unbounded = DensityEstimate(x, κ_const)\n\n(κ = round(κ_const; digits=1), leaked_mass = round(cdf(d_unbounded, 0.0); digits=4))\n\nleaked_mass is cdf(d_unbounded, 0.0), the fitted probability of landing on the wrong side of the true edge: about one percent of the fit's mass sits below x = 0, where the true density is exactly zero. select_support finds a left wall that removes this directly:\n\nr = select_support(x)\nd_bounded = DensityEstimate(x, r.κ; support = r.support)\n\n(wall = round(r.support[1]; digits=5), κ = round(r.κ; digits=2), cdf_at_wall = cdf(d_bounded, r.support[1]))\n\ncdf_at_wall is exactly 0, not merely small: mass cannot cross a natural boundary. Note also that the selected scale drops to about a fifth of κ_const — the unbounded fit was over-sharpening to fight the leak, and no longer needs to once the wall is doing that job (the search re-optimizes κ at every boundary candidate; see select_support's docstring). The wall itself sits close to the data:\n\nspacing = PenalizedDensity._edge_spacing(sort(x), :left)   # mean spacing of the 10 points nearest the edge\ngap = minimum(x) - r.support[1]\nround(gap / spacing; digits=2)\n\nwhich is the search's hard floor: select_support never places a wall closer than five mean edge-spacings to the data, because a boundary any closer reflects the nearest interior points back onto themselves and inflates their leave-one-out likelihood on any sample, edge or not, not only this one. On a genuine hard edge this floor is usually where the search lands, as it does here; a smooth sample has no edge to find in the first place, and the search says so plainly by returning the unbounded candidate:\n\nxg = randn(1500)\nrg = select_support(xg)\n\n(support = rg.support, κ_matches_plain_selection = rg.κ === select_kappa_kl(xg))\n\nsupport == (-Inf, Inf) and the returned κ is not merely close to select_kappa_kl(xg) but identical to it — the unbounded candidate always competes, and here it wins outright.\n\nTo combine a boundary with a varying scale, pass the chosen support on to select_kappa_adaptive so that its own search runs on the bounded domain:\n\nκ_composed = select_kappa_adaptive(x; support = r.support)\nd_composed = DensityEstimate(x, κ_composed; support = r.support)\n\n(α = κ_composed.α, c = round(κ_composed.c; digits=2))\n\nWe can test whether this fit is a genuine improvement by generating independent data from the same underlying distribution:\n\nxtest = -log.(1 .- rand(N))\nloglik(d) = mean(log.(d.(xtest)))\n\n(unbounded = round(loglik(d_unbounded); digits=3),\n bounded = round(loglik(d_bounded); digits=3),\n bounded_and_adaptive = round(loglik(d_composed); digits=3))\n\nThe wall accounts for essentially all of the gain here: once it has absorbed the edge's leak, little irregularity is left in an otherwise-exponential interior, and the two scores agree to within the noise of a fresh N-point sample. The picture below makes the boundary's effect on the fit itself concrete — the unbounded estimate (blue) trailing off below x = 0 where the true density (dashed) is zero, against the bounded estimate (crimson), pinned flat at the wall:\n\n(Image: An exponential jump edge: leaking mass past x=0 versus a wall that stops it exactly)","title":"Fitting a hard edge"},{"category":"section","location":"tutorial/#Goodness-of-fit","page":"Tutorial","text":"Because the estimate is a genuine likelihood fit, you can ask how well a specific model distribution describes the data. Using the fit d from above, chisq is a robust, binning-free chi^2 (the squared Hellinger distance between a trial density and the data):\n\nwrong(x) = exp(-((x - 0.5) / 2.5)^2 / 2) / (2.5 * sqrt(2π))   # a single broad Gaussian\n\n(correct = round(chisq(d, truepdf); digits = 1),\n incorrect = round(chisq(d, wrong); digits = 1))\n\nThe true mixture yields a far smaller chi^2 than the mismatched single Gaussian. pvalue turns the statistic into a significance under the reference distribution of chi^2 — the exact finite-N law (a generalized chi-squared), computed by default:\n\n(p_correct = round(pvalue(d, truepdf); digits = 3),\n p_incorrect = round(pvalue(d, wrong); digits = 6))\n\nThe single Gaussian is decisively rejected; the true mixture is not. chisq_pdf / chisq_ccdf give the reference density and upper-tail probability, and expected_chisq its mean. To test many trial densities against one fit, build the reference once with chisq_reference and reuse it:\n\nref = chisq_reference(d)\n(mean = round(expected_chisq(ref); digits = 2),\n p_correct = round(pvalue(ref, chisq(d, truepdf)); digits = 3))\n\nThe exact law is a per-call integral (Imhof inversion): accurate, but tens of milliseconds apiece. Passing method = :largeN selects instead the closed-form inverse-Gaussian (Wald) shape of the original paper's large-N limit, parameterized by the exact mean expected_chisq; it costs microseconds, so a large batch of trial densities against one fit is far cheaper. Anchored to the exact mean, it tracks the exact tail closely rather than overstating it:\n\n(exact = round(pvalue(d, truepdf); digits = 3),\n largeN = round(pvalue(d, truepdf; method = :largeN); digits = 3))\n\nAll of this — the exact law and the :largeN shape alike — works unchanged on a fit at a varying scale or a finite support: both are read from the same reference the fit assembles, in the same O(N).","title":"Goodness of fit"},{"category":"section","location":"tutorial/#Entropy-and-negentropy","page":"Tutorial","text":"Because the fit is a smooth density, its information content follows in closed form. entropy is a plug-in estimate of the differential entropy H(Q) = -int Q ln Q, and negentropy is the entropy deficit relative to the Gaussian of the same mean and variance — a scale-free measure of how non-Gaussian the density is, zero for a Gaussian and positive otherwise:\n\n(H = round(entropy(d); digits = 3), J = round(negentropy(d); digits = 3))\n\nThe bimodal fit is strongly non-Gaussian, so its negentropy is well above zero. negentropy is invariant to shifting and rescaling the data (with the matching kappa mapsto kappaa), so it measures shape alone.\n\nBoth quantities also take a second argument of held-out points — data that did not enter the fit — in which case ln hat Q is scored there and the Gaussian reference uses the held-out sample's own variance:\n\nxeval = [rand() < comps[1].w ? comps[1].μ + comps[1].σ * randn() :\n                               comps[2].μ + comps[2].σ * randn() for _ in 1:1000]\n(H = round(entropy(d, xeval); digits = 3), J = round(negentropy(d, xeval); digits = 3))\n\nThe held-out negentropy is close to the in-sample value here because the fit generalizes well. Scoring on points that did not build the fit is what makes the held-out form informative: unlike the in-sample value, it is not inflated by concentrating the density onto the fit sample, the same reason select_kappa_cv cross-validates.\n\nThe derivatives of the log density are exposed directly: logdensity_eval_gradient gives partial ln hat Q(y)partial y in closed form, and logdensity_node_gradient gives the gradient of a weighted sum of log densities with respect to the node positions, by an implicit-function adjoint that reuses the fit's factored Hessian in a single extra tridiagonal solve. Both are documented in the API reference.","title":"Entropy and negentropy"}]
}
